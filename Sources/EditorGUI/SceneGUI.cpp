#include "EditorGUI/SceneGUI.hpp"

using namespace Maths;
using namespace EditorGUI;

void SceneGUI::DoUpdate()
{
	if (isOnFocus)
	{
		// scene camera movement

	}

	//debugCamera.Render((*currentScene)->GetModelList(), size - Vec2(10, 35));

	//ImGui::Image((ImTextureID)debugCamera.GetRenderTextureKey(), ImVec2(size.x - 10, size.y - 35), ImVec2(0, 1), ImVec2(1, 0));


	//debugCamera.OnGUI();
	return;
}
